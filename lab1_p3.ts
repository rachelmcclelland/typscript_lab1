function numOfChars(value) {

    count = value.length;

    return count;
}

let count:number;
let value:String = "test 1";

console.log("Number of characters in the string (including spaces) is: " + numOfChars(value))